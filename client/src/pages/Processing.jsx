export default function Processing() {
  return (
    <div className="card p-4">
      <div className="card-title">LED Asset Processing</div>
      <p className="mt-2 text-sm text-text-secondary">
        The main run screen (discovery, validation, status, sequence preview, distribution) is implemented in
        Stage 15, after the underlying services (Stages 5-14) exist.
      </p>
    </div>
  );
}
